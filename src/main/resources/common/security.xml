<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:sec="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
    http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <sec:global-method-security secured-annotations="enabled" pre-post-annotations="enabled" />
    <sec:http pattern="/**" use-expressions="true">
        <sec:csrf/>
        <sec:form-login login-page="/signin" authentication-success-handler-ref="customLoginHandler" authentication-failure-handler-ref="customLoginFailureHandler"/>
        <sec:access-denied-handler ref="auditAccessDeniedHandler"/>

        <sec:headers>
            <sec:frame-options policy="SAMEORIGIN"/>
            <sec:content-type-options/>
            <sec:hsts include-subdomains="true" max-age-seconds="31536000"/>
            <sec:header name="Content-Security-Policy" value="default-src 'self'; img-src 'self' data:; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'"/>
            <sec:header name="Referrer-Policy" value="same-origin"/>
        </sec:headers>

        <sec:logout logout-url="/signout" invalidate-session="true" delete-cookies="JSESSIONID"/>
        <sec:session-management session-fixation-protection="migrateSession">
            <sec:concurrency-control max-sessions="3" error-if-maximum-exceeded="false" expired-url="/"/>
        </sec:session-management>
        <sec:intercept-url pattern="/student/**" access="hasAnyRole('ROLE_STUDENT', 'ROLE_ADMIN')" />
        <sec:intercept-url pattern="/professor/**" access="hasAnyRole('ROLE_PROFESSOR', 'ROLE_ADMIN')" />
        <sec:intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
        <sec:intercept-url pattern="/board/**" access="hasRole('ROLE_USER')" />
        <sec:intercept-url pattern="/" access="hasRole('ROLE_USER')" />
        <sec:intercept-url pattern="/**" access="permitAll" />
    </sec:http>

    <bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
    <sec:authentication-manager>
        <sec:authentication-provider user-service-ref="userService">
            <sec:password-encoder ref="passwordEncoder" />
        </sec:authentication-provider>
    </sec:authentication-manager>
</beans>